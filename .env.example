# ========================================
# Driver Finance App - Environment Configuration
# ========================================
# 
# IMPORTANT: Copy this file to .env and update with your actual values
# NEVER commit .env to version control!
#

# ========================================
# Database Configuration (REQUIRED)
# ========================================
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_secure_password_min_8_chars
POSTGRES_SERVER=db
POSTGRES_PORT=5432
POSTGRES_DB=driver_finance_db

# Optional: Full database URL (if set, overrides individual params)
# DATABASE_URL=postgresql+asyncpg://user:password@host:port/database


# ========================================
# Security Configuration (REQUIRED)
# ========================================
# Generate with: openssl rand -hex 32
SECRET_KEY=your_secret_key_here_minimum_32_characters_required

# JWT Configuration
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60


# ========================================
# First Superuser (REQUIRED)
# ========================================
FIRST_SUPERUSER_EMAIL=admin@yourdomain.com
FIRST_SUPERUSER_PASSWORD=secure_password_min_12_chars
FIRST_SUPERUSER_FULL_NAME=Admin User


# ========================================
# Environment & CORS
# ========================================
# Environment: development or production
ENVIRONMENT=production

# Frontend URL for CORS (set to specific URL in production)
# Use * for development, specific domain for production
FRONTEND_URL=https://your-frontend-url.com


# ========================================
# Quick Start Guide
# ========================================
# 
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Generate a secure SECRET_KEY:
#    openssl rand -hex 32
#
# 3. Update all REQUIRED fields above
#
# 4. For production deployment:
#    - Set ENVIRONMENT=production
#    - Set FRONTEND_URL to your actual frontend domain
#    - Use strong, unique passwords (min 12 chars)
#    - Never use default values
#
# 5. Verify configuration:
#    docker-compose up -d
#
